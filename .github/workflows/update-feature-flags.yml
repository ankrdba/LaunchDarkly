name: Evaluate LaunchDarkly Flags

on:
  push:
    branches:
      - master

jobs:
  eval-flags:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Evaluate flags
        id: flags
        uses: launchdarkly/gha-flags@v1.0.1
        with:
          sdk-key: ${{ secrets.LAUNCHDARKLY_SDK_KEY }}
          flags: |
            show-textbox,false
            developer-feature,false
            tester-feature,false

      - name: Print flag values
        run: |
          echo "show-textbox: ${{ steps.ld.outputs.show-textbox }}"
          echo "developer-feature: ${{ steps.ld.outputs.developer-feature }}"
          echo "tester-feature: ${{ steps.ld.outputs.tester-feature }}"
      - name: Debug outputs
        run: |
          echo "Outputs: ${{ toJson(steps.ld.outputs) }}"
